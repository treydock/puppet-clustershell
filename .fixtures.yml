fixtures:
  repositories:
    stdlib:
      repo: git://github.com/puppetlabs/puppetlabs-stdlib.git
      ref: 4.3.1
    epel:
      repo: git://github.com/stahnma/puppet-module-epel.git
      ref: 1.0.0
    concat:
      repo: git://github.com/puppetlabs/puppetlabs-concat.git
  symlinks:
    clustershell: "#{source_dir}"
